# Handoff - Company Profile v2 / BPR Karawang

Tanggal: 2026-05-25

## Status Terakhir

Project Rakit sudah berjalan di:

```txt
/var/www/html/company-profile-v2
```

URL lokal:

```txt
http://company-profile-v2.test
```

## Yang Sudah Selesai

### Setup Project

- Install Rakit v0.9.9.
- Konfigurasi Nginx untuk `company-profile-v2.test`.
- Pretty URL aktif tanpa `index.php`.
- Dotenv sudah ditambahkan.
- Tailwind CSS sementara memakai CDN untuk development.
- Path sensitif sudah diamankan via Nginx:
  - `/application/`
  - `/system/`
  - `/vendor/`
  - `/storage/`
  - `/packages/`
  - `/composer.json`
  - `/composer.lock`
  - `/.env`

### Dokumentasi

- `docs/PRD.md`
- `docs/ANALISIS-BPR-KARAWANG.md`
- `docs/SITEMAP-WIREFRAME.md`
- `docs/TAILWIND-CSS.md`
- `docs/AUDIT-2026-05-25.md`
- `docs/AUDIT-SECURITY-RECHECK-2026-05-25.md`
- `docs/ADMIN-PACKAGE.md`

### Halaman Public

Route sudah ada dan berjalan:

- `/`
- `/tentang-kami`
- `/produk-layanan`
- `/produk/tahara`
- `/pengurus`
- `/kontak`

Semua route terakhir dites HTTP 200.

### UI / Frontend

- Navbar responsive sudah diperbaiki dengan hamburger menu mobile.
- Active state navbar sudah aktif.
- Semua view pakai class Tailwind standar, tanpa custom helper class.
- Copywriting dummy sudah dirapikan agar lebih profesional dan aman.

### Admin Package

Admin package lokal sudah dibuat di:

```txt
packages/admin/
```

Route admin:

- `/admin`
- `/admin/login`
- `/admin/logout`
- `/admin/dashboard`
- `/admin/settings`
- `/admin/company-profile`
- `/admin/products`
- `/admin/management`

Auth sementara dari `.env`:

```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-admin
```

Setting admin tersimpan di:

```txt
storage/admin/settings.json
```

Admin sudah dites:

- Login page 200.
- Dashboard redirect ke login jika belum login.
- Login berhasil dengan credential `.env`.
- Submit settings berhasil update JSON.

## Update 2026-06-03

- Helper public setting sudah dibuat di `application/helpers/myhelper.php`:
  - `public_settings()` sekarang membaca tabel `site_settings` dengan fallback default.
  - `public_phone_href()`, `public_whatsapp_href()`, dan `public_safe_url()` untuk link kontak.
- Layout public sudah memakai setting admin untuk meta description dan title website.
- Navbar/footer sudah membaca nama perusahaan, tagline, telepon, email, alamat, dan jam layanan dari setting admin.
- Halaman kontak sudah membaca alamat, telepon, email, jam layanan, WhatsApp, dan Google Maps URL dari setting admin.
- CRUD produk tahap awal sudah dibuat:
  - Admin view `packages/admin/views/products.blade.php` untuk tambah, edit, hapus produk.
  - Route admin `GET/POST /admin/products` dan `POST /admin/products/delete`.
  - Data produk tersimpan di tabel `products`.
  - Public `/produk-layanan` membaca daftar produk dari database via `public_products()`.
  - Test tambah dan hapus produk via admin: OK.
- CRUD pengurus tahap awal sudah dibuat:
  - Admin view `packages/admin/views/management.blade.php` untuk tambah, edit, hapus pengurus.
  - Route admin `GET/POST /admin/management` dan `POST /admin/management/delete`.
  - Data pengurus tersimpan di tabel `management`.
  - Public `/pengurus` membaca data dari database via `public_management_by_group()`.
  - Test tambah dan hapus pengurus via admin: OK.
- Migrasi storage konten dari JSON ke MySQL sudah dilakukan:
  - Config database Rakit membaca `DB_CONNECTION`, `DB_HOST`, `DB_DATABASE`, `DB_USERNAME`, `DB_PASSWORD`, dan `DB_CHARSET` dari `.env`.
  - Tabel dibuat: `site_settings`, `company_profile`, `products`, `management`.
  - Data awal dimigrasikan dari JSON lama ke tabel MySQL.
  - File JSON di `storage/admin/*.json` dibiarkan sebagai backup legacy, bukan sumber data utama.
- Auth admin sudah dipindahkan ke database:
  - Tabel dibuat: `admin_users`.
  - User admin awal diseed dari credential `.env` dan password disimpan dengan `password_hash()`.
  - Login `/admin/login` sekarang membaca tabel `admin_users` dan validasi via `password_verify()`.
  - Session menyimpan `admin_user_id` dan `admin_username`.
  - Halaman baru `/admin/account` untuk ubah username, nama tampilan, dan password.
  - `ADMIN_USERNAME`/`ADMIN_PASSWORD` di `.env` sekarang hanya legacy/seed awal, bukan sumber auth runtime.
- Form kontak sudah memakai backend handler dan database:
  - Tabel dibuat: `contact_messages`.
  - Public `POST /kontak` menyimpan nama, email/telepon, subjek, pesan, IP, user agent, status baca.
  - Halaman admin baru `/admin/contact-messages` untuk daftar pesan masuk.
  - Halaman detail `/admin/contact-messages/{id}` untuk membaca isi pesan.
  - `POST /admin/contact-messages/status` untuk tandai sudah/belum dibaca.
  - `POST /admin/contact-messages/delete` untuk hapus pesan.
  - Menu “Pesan Kontak” sudah ditambahkan di sidebar admin.
- Hardening form/admin tahap awal sudah dibuat:
  - Helper `csrf_token()`, `csrf_field()`, `csrf_check()` di `application/helpers/myhelper.php`.
  - Semua form POST public/admin memakai CSRF token.
  - Semua route POST public/admin memverifikasi CSRF token.
  - Form kontak memakai rate limit session 30 detik.
  - Input penting dibatasi panjangnya via `text_limit()` sebelum masuk database.
- Detail produk sudah dibuat dinamis berdasarkan slug:
  - Helper `public_product_by_slug()` dan `public_product_initial()`.
  - Route public `GET /produk/{slug}` membaca data produk dari tabel `products`.
  - View baru `application/views/products/detail.blade.php`.
  - Link di `/produk-layanan` sekarang mengarah ke `produk/{slug}` untuk semua produk.
  - Produk tidak ditemukan mengembalikan HTTP 404.
- Upload/foto pengurus sudah dibuat:
  - Kolom baru `management.photo_path`.
  - Folder upload: `assets/uploads/management`.
  - Admin `/admin/management` bisa upload foto JPG/PNG/WEBP maksimal 2MB.
  - Admin bisa hapus foto saat menyimpan data pengurus.
  - Public `/pengurus` menampilkan foto jika tersedia, fallback ke inisial jika kosong.
  - Folder upload diberi ACL untuk user web server `www-data` agar bisa menulis tanpa permission 777.
- Admin UX pesan kontak sudah dirapikan:
  - Search pesan berdasarkan nama, kontak, subjek, atau isi pesan.
  - Filter status: semua, belum dibaca, sudah dibaca.
  - Pagination 10 pesan per halaman.
  - Badge jumlah pesan belum dibaca di sidebar.
  - Aksi cepat tandai Read/Unread dari daftar pesan.
  - Detail pesan punya tombol tandai sudah/belum dibaca dan hapus.
- Edit profil perusahaan tahap awal sudah dibuat:
  - Admin view `packages/admin/views/company-profile.blade.php` untuk edit konten Tentang Kami.
  - Route admin `GET/POST /admin/company-profile`.
  - Data profil tersimpan di tabel `company_profile`.
  - Public `/tentang-kami` membaca data dari database via `public_company_profile()`.
  - Test simpan profil via admin dan tampil di public: OK.
- Verifikasi terakhir:
  - `php -l` untuk file helper/view/routes yang diubah: OK.
  - Route public `/`, `/tentang-kami`, `/produk-layanan`, `/produk/tahara`, `/pengurus`, `/kontak`: HTTP 200.
  - Admin login database dan halaman `/admin/settings`, `/admin/products`, `/admin/management`, `/admin/company-profile`, `/admin/contact-messages`, serta `/admin/account`: HTTP 200 setelah login.
  - Test tambah/hapus produk via MySQL: OK.
  - Test tambah/hapus pengurus via MySQL: OK.
  - Test simpan settings dan company profile via MySQL: OK.
  - Test update akun admin via `/admin/account`: OK.
  - Test submit kontak public, lihat detail admin, mark as read, dan hapus pesan: OK.
  - Test CSRF public/admin tanpa token ditolak: OK.
  - Test rate limit kontak submit cepat kedua ditolak: OK.
  - Test detail produk `/produk/tahara`: HTTP 200.
  - Test produk dummy `/produk/test-dinamis`: HTTP 200 dan tampil sesuai data DB, lalu data test dihapus.
  - Test produk tidak ada `/produk/tidak-ada`: HTTP 404.
  - Test upload foto pengurus via admin: OK.
  - Test foto pengurus tampil di public `/pengurus`: OK.
  - Test hapus foto pengurus via admin: OK.
  - File foto test sudah dihapus dan data `photo_path` dikosongkan kembali.
  - Test admin pesan kontak `/admin/contact-messages`: HTTP 200.
  - Test search pesan kontak `?q=Tabungan`: HTTP 200.
  - Test filter status `?status=unread` dan `?status=read`: HTTP 200.
  - Test pagination `?page=2`: HTTP 200.
  - Test mark read via `POST /admin/contact-messages/status`: OK.
  - Data dummy UX test sudah dihapus.
  - Jumlah data DB terakhir: `site_settings=8`, `company_profile=7`, `products=1`, `management=4`, `admin_users=1`, `contact_messages=0` setelah pesan test dihapus.

## Catatan Penting

Password admin masih default development. Harus diganti sebelum dipakai serius.

## Lanjut Besok

Prioritas berikutnya:

1. ~~Hubungkan data public website ke `storage/admin/settings.json`.~~ Selesai 2026-06-03.
   - Navbar/footer baca nama, telepon, email, jam layanan.
   - Halaman kontak baca alamat, telepon, email, jam layanan, WhatsApp, Google Maps.

2. ~~Buat helper data setting agar view public mudah membaca data admin.~~ Selesai 2026-06-03.

3. Setelah setting public terhubung, lanjut CRUD bertahap:
   - Produk — CRUD tahap awal selesai dan sudah dites.
   - Pengurus — CRUD tahap awal selesai dan sudah dites.
   - Profil perusahaan — edit konten tahap awal selesai dan sudah dites.

4. ~~Tentukan behavior form kontak.~~ Selesai 2026-06-03.
   - Backend handler sederhana menyimpan pesan ke tabel `contact_messages`.
   - WhatsApp tetap tersedia sebagai CTA alternatif jika nomor WhatsApp diisi di settings.

5. ~~Ganti password admin default di `.env`.~~ Auth sudah pindah ke database; ganti password dari `/admin/account`, lalu `ADMIN_USERNAME`/`ADMIN_PASSWORD` di `.env` bisa dihapus/diabaikan sebagai legacy.

6. Setelah semua stabil, ganti Tailwind CDN ke build production lokal.
